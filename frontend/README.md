# CyberCity ICS/OT — Frontend

React + TypeScript + Vite application for the CyberCity training platform.

## Development

```bash
npm install
npm run dev
```

Opens on http://localhost:3000. Proxies `/api` and `/socket.io` to the backend at localhost:8000.

## Build

```bash
npm run build
```

Output goes to `dist/`.
